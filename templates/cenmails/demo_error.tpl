Ooops... Something went wrong:

Job running on: ${host}
Job identifier: ${jobid}
${mtool_info}
The footprint defaults were:
${fpdefaults}

# check variables access and case insensitivity

An environment variable: home = ${home}
An environment variable: HOME = ${HOME}

An extra variable: ${extra}
An EXTRA variable: ${EXTRA}
